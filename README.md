# homebrew-tap

This is a Homebrew tap repository.

Structure:

- `Formula/` — one Ruby formula file per line, e.g. `Formula/blokebot.rb`

Usage:

```bash
brew tap <owner>/homebrew-tap
brew install <formula-name>
```
